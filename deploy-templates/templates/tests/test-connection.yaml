apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "javascript-pnpm-express.fullname" . }}-test-connection"
  labels:
    {{- include "javascript-pnpm-express.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "javascript-pnpm-express.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
